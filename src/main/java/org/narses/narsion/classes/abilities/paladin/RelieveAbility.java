package org.narses.narsion.classes.abilities.paladin;

import net.minestom.server.entity.Player;
import org.narses.narsion.NarsionServer;

public class RelieveAbility {
	public static void activate(NarsionServer server, Player player) {
		/*
		for (Player anotherPlayer : player.getViewers()) {
			
			// TODO: Check if player is in guild/party
			if (player.getPosition().getDistance(anotherPlayer.getPosition()) < 10) {
				
				// TODO: Update to new effect system
				// ((NarsesPlayer) player).addEffect(NarsesEffects.REGENERATION, 1, 300); // 300 = 15 seconds
			}
		}

		 */
	}
}
