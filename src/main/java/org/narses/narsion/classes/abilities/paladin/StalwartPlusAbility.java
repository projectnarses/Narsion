package org.narses.narsion.classes.abilities.paladin;

import net.minestom.server.entity.Player;
import org.narses.narsion.NarsionServer;

public class StalwartPlusAbility {
	public static void activate(NarsionServer server, Player player) {

		/*
		// TODO: Update to new attack system
		
		// Get position
		final Position position = player.getPosition().clone();

		// Get surrounding players
		for (final Player somePlayer : player.getViewers()) {
			final double distance = position.getDistance(somePlayer.getPosition());
			if ((distance <= 10) && (somePlayer.getEntityId() != player.getEntityId())) {
				
				
				
				// Set fortifiable
				// ((Fortifiable) somePlayer).addFortifier(player);

				// Schedule to remove in 30 seconds
				// MinecraftServer.getSchedulerManager()
				// 		.buildTask(() -> ((Fortifiable) somePlayer).removeFortifier(player)).delay(30, TimeUnit.SECOND)
				// 		.schedule();
			}
		}

		 */
	}
}
