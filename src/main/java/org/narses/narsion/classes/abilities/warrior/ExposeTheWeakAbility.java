package org.narses.narsion.classes.abilities.warrior;

import net.minestom.server.entity.Player;
import org.narses.narsion.NarsionServer;

public class ExposeTheWeakAbility {
	public static void activate(NarsionServer server, Player player) {
/*
		player.getViewers()
			.stream()
			// Convert to narses plyaers
			.filter(NarsesPlayer.class::isInstance)
			// Sort players by health
			.min(Comparator.comparing(Player::getHealth))
			.ifPresent(lowestPlayer -> {
				// TODO: Update to new effect system
				// ((NarsesPlayer) lowestPlayer).addEffect(NarsesEffects.EXPOSED, 1, 1000);
			});
			*/
	}
}
