plugins {
    id 'java'
    id "com.github.johnrengelman.shadow" version "6.1.0"
}

group 'org.narses'
version 'DEVELOPMENTBUILD-ALPHA'

repositories {
    mavenCentral()

    // Minestom repos
    maven { url 'https://repo.spongepowered.org/maven' }
    maven { url 'https://jitpack.io' }
}

dependencies {
    // Minestom (new block api)
    implementation 'com.github.Minestom:Minestom:new-block-api-SNAPSHOT'

    // Entity library
    implementation 'com.github.projectnarses:NarsionEntities:0f426215bb'

    // Item library
    implementation 'com.github.KrystilizeNevaDies:Itemize:e8fbd1a880'

    // Toml config library
    implementation 'com.github.mwanji:toml4j:4d206e4af6'
}

jar {
    manifest {
        attributes 'Main-Class': 'org.narses.narsion.BootstrapDevServer',
                "Multi-Release": true
    }
}